--- android_project/app/src/main/java/com/example/ridebuddy/routing/MockBRouterEngine.kt
+++ android_project/app/src/main/java/com/example/ridebuddy/routing/MockBRouterEngine.kt
@@ -6,7 +6,7 @@
 import org.mapsforge.core.model.LatLong

 class MockBRouterEngine : OfflineRoutingEngine {
-    override suspend fun calculateRoute(start: LatLong, destination: LatLong): List<LatLong> {
+    override suspend fun calculateRoute(start: LatLong, destination: LatLong): RoutingResult {
         return withContext(Dispatchers.IO) {
             // Simulate offline calculation delay
             delay(1000)
@@ -23,7 +23,7 @@
             }

             route.add(destination)
-            route
+            RoutingResult(route, emptyList())
         }
     }
 }
