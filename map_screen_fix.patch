--- android_project/app/src/main/java/com/example/ridebuddy/ui/MapScreen.kt
+++ android_project/app/src/main/java/com/example/ridebuddy/ui/MapScreen.kt
@@ -456,6 +456,29 @@
             }
         }
+
+        if (showProDialog) {
+            ModalBottomSheet(
+                onDismissRequest = { showProDialog = false }
+            ) {
+                Column(modifier = Modifier.padding(16.dp)) {
+                    Text("Ridebuddy Pro", style = MaterialTheme.typography.headlineMedium)
+                    Spacer(modifier = Modifier.height(8.dp))
+                    Text("Unlock premium features like SMS Fallback Sync and Solar Telemetry.")
+                    Spacer(modifier = Modifier.height(16.dp))
+                    Button(
+                        onClick = {
+                            viewModel.launchBillingFlow(context as Activity) { _ -> }
+                            showProDialog = false
+                        },
+                        modifier = Modifier.fillMaxWidth()
+                    ) {
+                        Text("Purchase Now")
+                    }
+                    Spacer(modifier = Modifier.height(32.dp))
+                }
+            }
+        }
     }
 }
